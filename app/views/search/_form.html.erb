<%= search_form_for(@query, remote: true, method: :get, class:"d-flex") do |f| %>
    <%= f.search_field :title_or_body_or_user_email_i_cont_any, placeholder: "#{render'posts/count'}", class:"form-control me-2" %>
    <%= f.submit t(:search), class:"btn btn-outline-success"%>
<% end %>